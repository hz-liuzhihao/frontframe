import { createApp } from "vue";
import Vuex from "vuex";

createApp().use(Vuex);

const store = new Vuex.Store({
  state: {},
  mutations: {},
  actions: {},
  modules: {},
});

export default store;
